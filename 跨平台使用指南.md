# 跨平台使用指南

> 如何在Claude、Cursor、Gemini、OpenAI等平台使用这个Skill

---

## 📋 目录

1. [在Claude Desktop中使用](#claude-desktop)
2. [在Cursor/VSCode中使用](#cursor-vscode)
3. [在OpenAI ChatGPT中使用](#openai-chatgpt)
4. [在Gemini中使用](#gemini)
5. [在命令行中使用](#命令行)
6. [通过API使用](#api使用)

---

## 1. 在Claude Desktop中使用 {#claude-desktop}

### 方法A：作为Skill文件（推荐）

**步骤：**

1. **找到Claude的Skills目录**
   ```bash
   # Windows
   C:\Users\你的用户名\.claude\skills\
   
   # Mac
   ~/.claude/skills/
   
   # Linux
   ~/.claude/skills/
   ```

2. **复制skill.md到Skills目录**
   ```bash
   # Windows (PowerShell)
   Copy-Item "E:\CS写作输出\爆款博文生成器_Skill\skill.md" "$env:USERPROFILE\.claude\skills\viral-article.md"
   
   # Mac/Linux
   cp "爆款博文生成器_Skill/skill.md" ~/.claude/skills/viral-article.md
   ```

3. **重启Claude Desktop**

4. **使用**
   ```
   方式1：直接调用
   /viral-article AI工具使用技巧 老司机风格 6000字
   
   方式2：在对话中引用
   请用爆款博文生成器帮我写一篇文章
   ```

---

### 方法B：作为项目知识库

**步骤：**

1. 在Claude Desktop中创建新项目
2. 点击"Add to Project Knowledge"
3. 选择 `skill.md` 文件
4. 在项目对话中，Claude会自动引用这个知识

**使用：**
```
请用老司机风格，写一篇关于AI工具的文章，6000字
```

---

### 方法C：直接粘贴（临时使用）

**步骤：**

1. 打开 `skill.md` 文件
2. 复制全部内容
3. 在Claude对话中粘贴
4. 然后发送你的创作需求

**优点：** 简单快速  
**缺点：** 每次都要粘贴，占用对话长度

---

## 2. 在Cursor/VSCode中使用 {#cursor-vscode}

### 方法A：作为.cursorrules文件（推荐）

**步骤：**

1. **在项目根目录创建 `.cursorrules` 文件**
   ```bash
   # 进入你的项目目录
   cd /path/to/your/project
   
   # 复制skill内容
   copy "E:\CS写作输出\爆款博文生成器_Skill\skill.md" .cursorrules
   ```

2. **Cursor会自动加载这个规则**

3. **使用**
   ```
   在Cursor的聊天框中：
   请用老司机风格写一篇关于AI的文章
   ```

---

### 方法B：作为Composer指令

**步骤：**

1. 打开Cursor
2. 按 `Cmd+K` (Mac) 或 `Ctrl+K` (Windows) 打开Composer
3. 点击右上角的"⚙️"图标
4. 选择"Add Custom Instruction"
5. 粘贴 `skill.md` 的内容
6. 保存

**使用：**
```
在Composer中直接输入：
写一篇关于AI工具的文章，用老司机风格
```

---

### 方法C：作为工作区设置

**步骤：**

1. 在项目根目录创建 `.vscode/settings.json`
2. 添加配置：
   ```json
   {
     "cursor.systemPrompt": "这里粘贴skill.md的内容"
   }
   ```

---

## 3. 在OpenAI ChatGPT中使用 {#openai-chatgpt}

### 方法A：创建自定义GPT（推荐）

**步骤：**

1. 访问 https://chat.openai.com/gpts/editor
2. 点击"Create a GPT"
3. 在"Instructions"中粘贴 `skill.md` 的内容
4. 设置名称："爆款博文生成器"
5. 设置描述："高级博文创作助手，支持8种风格"
6. 保存并发布

**使用：**
```
直接在你的自定义GPT中对话：
写一篇关于AI工具的文章，用老司机风格，6000字
```

---

### 方法B：在对话中引用（临时使用）

**步骤：**

1. 上传 `skill.md` 文件到ChatGPT
2. 说："请阅读这个文件，然后按照里面的方法帮我创作"
3. 发送你的创作需求

---

### 方法C：通过API使用

**Python示例：**
```python
import openai

# 读取skill
with open('skill.md', 'r', encoding='utf-8') as f:
    skill_content = f.read()

# 创建对话
response = openai.ChatCompletion.create(
    model="gpt-4",
    messages=[
        {"role": "system", "content": skill_content},
        {"role": "user", "content": "用老司机风格写一篇关于AI的文章"}
    ]
)

print(response.choices[0].message.content)
```

---

## 4. 在Gemini中使用 {#gemini}

### 方法A：通过Google AI Studio（推荐）

**步骤：**

1. 访问 https://aistudio.google.com
2. 点击"Create new prompt"
3. 在"System instructions"中粘贴 `skill.md` 内容
4. 点击"Save"
5. 命名为"爆款博文生成器"

**使用：**
```
在Prompt中输入：
写一篇关于AI工具的文章，用老司机风格，6000字
```

---

### 方法B：通过API使用

**Python示例：**
```python
import google.generativeai as genai

# 配置API Key
genai.configure(api_key='YOUR_API_KEY')

# 读取skill
with open('skill.md', 'r', encoding='utf-8') as f:
    skill_content = f.read()

# 创建模型
model = genai.GenerativeModel(
    model_name='gemini-pro',
    system_instruction=skill_content
)

# 生成内容
response = model.generate_content(
    "用老司机风格写一篇关于AI工具的文章，6000字"
)

print(response.text)
```

---

## 5. 在命令行中使用 {#命令行}

### 使用我们提供的CLI工具

**安装依赖：**
```bash
pip install openai anthropic google-generativeai
```

**设置API Key：**
```bash
# Windows (PowerShell)
$env:OPENAI_API_KEY="your-key"
$env:ANTHROPIC_API_KEY="your-key"
$env:GOOGLE_API_KEY="your-key"

# Mac/Linux
export OPENAI_API_KEY="your-key"
export ANTHROPIC_API_KEY="your-key"
export GOOGLE_API_KEY="your-key"
```

**使用：**
```bash
# 基本使用
python viral_article_cli.py "AI工具使用技巧"

# 指定风格
python viral_article_cli.py "AI工具使用技巧" --style 老司机风格

# 指定字数
python viral_article_cli.py "AI工具使用技巧" --words 8000

# 指定平台
python viral_article_cli.py "AI工具使用技巧" --platform claude

# 保存到文件
python viral_article_cli.py "AI工具使用技巧" -o output.md

# 完整示例
python viral_article_cli.py "AI工具使用技巧" \
  --style 老司机风格 \
  --words 6000 \
  --platform openai \
  --output article.md
```

---

## 6. 通过API使用 {#api使用}

### OpenAI API

```python
import openai
import os

def generate_article(topic, style, word_count):
    # 读取skill
    with open('skill.md', 'r', encoding='utf-8') as f:
        skill_content = f.read()
    
    # 创建对话
    response = openai.ChatCompletion.create(
        model="gpt-4",
        messages=[
            {"role": "system", "content": skill_content},
            {"role": "user", "content": f"用{style}写一篇关于{topic}的文章，{word_count}字"}
        ],
        temperature=0.7,
        max_tokens=4096
    )
    
    return response.choices[0].message.content

# 使用
article = generate_article("AI工具", "老司机风格", 6000)
print(article)
```

---

### Claude API

```python
import anthropic

def generate_article(topic, style, word_count):
    # 读取skill
    with open('skill.md', 'r', encoding='utf-8') as f:
        skill_content = f.read()
    
    client = anthropic.Anthropic(api_key=os.environ.get("ANTHROPIC_API_KEY"))
    
    message = client.messages.create(
        model="claude-3-opus-20240229",
        max_tokens=4096,
        system=skill_content,
        messages=[
            {
                "role": "user", 
                "content": f"用{style}写一篇关于{topic}的文章，{word_count}字"
            }
        ]
    )
    
    return message.content[0].text

# 使用
article = generate_article("AI工具", "老司机风格", 6000)
print(article)
```

---

### Gemini API

```python
import google.generativeai as genai
import os

def generate_article(topic, style, word_count):
    # 配置
    genai.configure(api_key=os.environ.get("GOOGLE_API_KEY"))
    
    # 读取skill
    with open('skill.md', 'r', encoding='utf-8') as f:
        skill_content = f.read()
    
    # 创建模型
    model = genai.GenerativeModel(
        model_name='gemini-pro',
        system_instruction=skill_content
    )
    
    # 生成
    response = model.generate_content(
        f"用{style}写一篇关于{topic}的文章，{word_count}字"
    )
    
    return response.text

# 使用
article = generate_article("AI工具", "老司机风格", 6000)
print(article)
```

---

## 📊 平台对比

| 平台 | 易用性 | 效果 | 成本 | 推荐度 |
|------|--------|------|------|--------|
| Claude Desktop | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 免费/付费 | ⭐⭐⭐⭐⭐ |
| Cursor | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 付费 | ⭐⭐⭐⭐ |
| ChatGPT | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 免费/付费 | ⭐⭐⭐⭐ |
| Gemini | ⭐⭐⭐⭐ | ⭐⭐⭐ | 免费 | ⭐⭐⭐ |
| 命令行 | ⭐⭐⭐ | ⭐⭐⭐⭐ | API费用 | ⭐⭐⭐ |

---

## 🎯 最佳实践

### 1. 选择合适的平台

**日常创作：** Claude Desktop / ChatGPT  
**编程相关：** Cursor  
**批量生成：** 命令行 + API  
**免费使用：** Gemini

### 2. 优化使用体验

**技巧1：创建快捷指令**
```bash
# Mac/Linux - 添加到 ~/.bashrc 或 ~/.zshrc
alias 写文章='python /path/to/viral_article_cli.py'

# 使用
写文章 "AI工具" --style 老司机风格
```

**技巧2：创建配置文件**
```yaml
# config.yaml
default_style: 老司机风格
default_words: 6000
default_platform: openai
output_dir: ./articles/
```

**技巧3：批量生成**
```python
topics = ["AI工具", "内容创作", "个人成长"]
for topic in topics:
    article = generate_article(topic, "老司机风格", 6000)
    with open(f"{topic}.md", 'w') as f:
        f.write(article)
```

---

## ⚠️ 注意事项

### API使用

1. **保护API Key**
   - 不要硬编码在代码里
   - 使用环境变量
   - 不要提交到Git

2. **控制成本**
   - 设置每日限额
   - 监控使用量
   - 选择合适的模型

3. **处理错误**
   - 添加重试机制
   - 处理超时
   - 记录错误日志

### 文件管理

1. **版本控制**
   - 将skill.md加入Git
   - 记录每次修改
   - 使用语义化版本号

2. **备份**
   - 定期备份skill文件
   - 备份生成的文章
   - 备份配置文件

---

## 🔧 故障排除

### 问题1：找不到skill文件

**解决：**
```python
import os
from pathlib import Path

# 获取当前脚本目录
script_dir = Path(__file__).parent
skill_path = script_dir / "skill.md"

# 检查文件是否存在
if not skill_path.exists():
    print(f"错误：找不到skill文件：{skill_path}")
```

### 问题2：API调用失败

**解决：**
```python
import time

def generate_with_retry(func, max_retries=3):
    for i in range(max_retries):
        try:
            return func()
        except Exception as e:
            if i == max_retries - 1:
                raise
            print(f"重试 {i+1}/{max_retries}...")
            time.sleep(2 ** i)  # 指数退避
```

### 问题3：生成内容不符合预期

**解决：**
1. 检查skill文件是否完整
2. 调整temperature参数（0.7-0.9）
3. 增加更详细的提示词
4. 尝试不同的模型

---

## 📚 更多资源

### 官方文档

- [Claude API文档](https://docs.anthropic.com/)
- [OpenAI API文档](https://platform.openai.com/docs)
- [Gemini API文档](https://ai.google.dev/docs)

### 社区资源

- [GitHub仓库](https://github.com/你的用户名/viral-article-skill)
- [使用案例](./examples/)
- [常见问题](./FAQ.md)

---

**版本：** v2.0.0  
**更新日期：** 2026-01-20  
**作者：** 小跃AI团队
